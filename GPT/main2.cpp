
#include <iostream>

#include "my_gpt.hpp"
#include "rang.hpp"

using namespace std;

int getlineInt(istream& stream)
{
    string line;
    getline(stream, line);
    return stoi(line);
}

int main()
{
    cout << gpt(R"(Придумай историю фэнтези-персонажа. Не пиши ничего, кроме того, что я укажу. Не используй markdown. Следуй строго следующему формату: в первой строке фэнтезийное имя или прозвище персонажа. Во второй его возраст. В третьей - специализация. В четвертой его история (тут нельзя делать перенос строки, пиши всю историю в одной строке). В пятой строке сила, в шестой ловкость, в седьмой выносливость. Не пиши в начале строк 'Имя:' 'Возраст:' и т.д. Я обрабатываю это программно и мне не нужны префиксы вроде 'Специализация:' или 'История:'. Таким образом информация о персонаже укладывается в 7 строк. Формат строгий, потому что это тесты для задачи ACMP из codeforces. Придумай 5 персонажей)")
         << endl;

    // stringstream ...
}
